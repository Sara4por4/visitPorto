<div class="cta-card cta-card--plan">

   <div class="cta-card__bg" style="background-image:url({{$image}})"></div>

    <div class="cta-card__caption">
       <h4 class="cta-card__subtitle text text--caps text--bold">{{$subtitle}}</h4>
       <h3 class="cta-card__title section-title">{{$title}}</h3>
       <a href="{{$link}}" class="btn btn--white">Planear <span class="icon icon--compass"></span></a>
    </div>

</div>
